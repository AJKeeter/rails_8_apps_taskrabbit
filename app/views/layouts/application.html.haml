!!!
%html
%head
  %title Rails8AppsTaskrabbit
  = stylesheet_link_tag 'application', media: 'all'
  = javascript_include_tag 'application'
  = csrf_meta_tags

%body
  - Category.all.each do |category|
    = link_to category.name, tasks_path(category: category.name)

  = yield